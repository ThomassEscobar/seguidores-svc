@startuml C4-1-Contexto
!include <C4/C4_Context>

title ms-seguidores — C4 Nivel 1: Contexto

LAYOUT_WITH_LEGEND()

Person(actor, "Usuario / Creador", "Publica, sigue y conversa")
System(sys, "ms-seguidores", "Seguidores del caso caso12: orquesta las operaciones de negocio del dominio, garantiza la consistencia de los datos que le corresponden y publica los eventos que desencadenan el resto del proceso")

System_Ext(core, "ms-contenido", "Contenido del caso (servicio central)")
System_Ext(notif, "ms-mensajeria", "Mensajería del caso")
System_Ext(cognito, "Amazon Cognito", "Autenticación y JWT por rol")
System_Ext(bus, "Amazon EventBridge", "Bus de eventos de dominio")

Rel(actor, sys, "Usa el servicio", "HTTPS / REST + JWT")
Rel(sys, core, "Contenido del caso (servicio central)", "REST / eventos")
Rel_Back(notif, sys, "Mensajería del caso", "EventBridge / SQS")
Rel_Back(cognito, sys, "Autenticación y JWT por rol", "JWT")
Rel(sys, bus, "Bus de eventos de dominio", "AWS SDK")

SHOW_LEGEND()
@enduml
